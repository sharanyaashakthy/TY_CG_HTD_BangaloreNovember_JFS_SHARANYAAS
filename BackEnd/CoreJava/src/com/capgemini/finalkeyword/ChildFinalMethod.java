package com.capgemini.finalkeyword;

public class ChildFinalMethod extends FinalMethod {
	
	// final method gets inherited to the child class
	// but final method can't be overridden in the sub class. Bcos, overriding is all about changing 
	//the implementation in sub-class, but final is about keeping the content same.
}
